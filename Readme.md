

## 当前版本
首次尝试多目标优化问题，目标函数为 min(Cost)和 min(RA)

其中GB-ACO生成了一条最优解，然后使用多次扰动生成pareto解集，最终得到pareto前沿。

目前的方法比较简单，接下来我打算这么做：
1. 扩展算法为 GB-ACO；VNS；NSGA-II
2. 其中在GB-ACO中优化GB内部的目标函数，修改为真实的目标函数，而不是代理目标值（最短距离），最终生成两条最优解。然后由VNS扩展，NSGAII得到最终解集。

## 若方法成功，则计划在问题中引入时间窗口的限制，其中目标函数的 min(Cost) 中则会多了一项时间惩罚项；min(RA)保持不变

## 若时间窗口的引入可行，那么进一步优化问题的目标函数，修改为三目标函数：总成本，总焦虑，客户满意度

    其中客户满意度的相关评价函数需要进一步分析










